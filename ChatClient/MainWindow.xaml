<Window x:Class="ChatClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChatClient"
        mc:Ignorable="d"
        Title="Chat App" Height="500" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 10">
            <TextBox x:Name="UsernameBox" Width="200" Height="30"/>
            <Button Content="Login" Width="100" Margin="10 0 0 0" Click="Login_Click"/>
        </StackPanel>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="UsersList"
            Grid.Column="0"
            Height="20"
            VerticalAlignment="Top"
            Margin="0 0 10 0"
            DisplayMemberPath="Username"
            SelectionChanged="UsersList_SelectionChanged"/>
            <ListBox x:Name="MessagesList" Grid.Column="1"/>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0 10 0 0">
            <TextBox x:Name="MessageInput" Width="250" Height="30"/>
            <Button Content="Send" Width="100" Margin="10 0 0 0" Click="Send_Click"/>
        </StackPanel>
    </Grid>
</Window>
